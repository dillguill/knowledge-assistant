${GROUNDING}

${SOURCES}

Below is a drafted research brief. List the claims in it that are NOT
supported by the source material above, or that carry a citation label the
sources do not contain. Quote each claim briefly. An empty list is a valid and
common answer.

Reply with only this JSON object:
{"unsupported_claims": ["..."]}

Brief:
${BRIEF}
