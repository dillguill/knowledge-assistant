${GROUNDING}

${SOURCES}

You are drafting one section of a research brief on: ${TOPIC}

Section title: ${SECTION_TITLE}
Supporting sources: ${SECTION_LABELS}

Write this section in markdown, starting with a `## ${SECTION_TITLE}` heading.
Cite every factual claim inline with its source label, e.g. [S1]. If the
sources do not support a claim, leave it out rather than asserting it.
Write the section only — no preamble, no other sections.
