${GROUNDING}

${SOURCES}

Topic: ${TOPIC}
Sub-questions:
${QUESTIONS}

Outline a brief of at most ${MAX_SECTIONS} sections that answers the
sub-questions from the source material above. For each section, list the
source labels (e.g. "S1") that actually support it. Do not invent labels that
are not in the source material.

Reply with only this JSON object:
{"sections": [{"title": "...", "source_labels": ["S1"]}]}
